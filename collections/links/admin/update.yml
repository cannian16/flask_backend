info:
  name: update
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/links/update"
  headers:
    - name: X-API-KEY
      value: "{{X-API-KEY}}"
  body:
    type: json
    data: |-
      {
        "id" : "1",
        "name":"",
        "url":"",
        "icon":"",
        "description":""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
