info:
  name: delete
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/links/delete"
  headers:
    - name: X-API-KEY
      value: "{{X-API-KEY}}"
  body:
    type: json
    data: |-
      {
        "id" : 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
