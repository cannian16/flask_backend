info:
  name: delete
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/songs/delete"
  headers:
    - name: X-API-KEY
      value: "{{X-API-KEY}}"
  body:
    type: json
    data: |-
      {
        "id" : "1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
