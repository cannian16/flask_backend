info:
  name: get
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/messages/get?page=1&limit=20"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
